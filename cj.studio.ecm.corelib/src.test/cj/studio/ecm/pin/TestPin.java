package cj.studio.ecm.pin;


public class TestPin {
	/*public void testPin(){
		Assembly assembly1=null;
		ServiceCollection<IOutputPin> outs=assembly1.getWorkbin().getPart(IOutputPin.class);
		
		Assembly assembly2=null;
		ServiceCollection<IInputPin> ins=assembly2.getWorkbin().getPart(IInputPin.class);
//		outs.get(0).pin(ins.get(0));//这是一种连接式访问，消息由程序集1内部产生，传入程序集2处理
		IInputPin in=ins.get(1);//这是一种外部调用的非连接访问，消息由调用者发送，传入程序集1处理
		Frame reqFrame=null,resFrame=null;
//		in.flow(reqFrame,resFrame);
//		Instruction[] inst=in.enumInstruction();
//		Instruction saveUser=in.getInstruction("saveUser");
//		saveUser.execute(user);
		
	}
	public void testPin2(){
//		IPin pin=new CollectionInputPin();
//		ISink sink=null;
//		pin.plugLast("",sink);
//		pin.flow(null, null);
		
	}
	public void testGraph(){
		IGraph graph=null;
		graph.setCreator(null);
//		graph.pin.plug("encode",sink);
//		Pin pin=graph.findPin("session2");
//		pin.plugFirst("log",sink2);
	}*/
}
